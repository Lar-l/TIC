#include <iostream>
#include <cmath>
using namespace std;

int main()
{/* int numarDeVerificat;
    cin >> numarDeVerificat;
    for(int divizor = 1; divizor <= sqrt(numarDeVerificat); divizor++){
        if(numarDeVerificat % divizor == 0){
            if(divizor != sqrt(numarDeVerificat)){
                cout << divizor << " " << numarDeVerificat / divizor << " ";
            }
            else{
                cout << divizor;
            }
        }
    }
    return 0;
}
